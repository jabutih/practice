#include <stdio.h>
// any files that ends in .h are header files
// stdio is "standard io"

int main(void)
// int - integer, the whole numbers

{
    printf("Hello, world!\n");
    // printf - prints and format, it's the function, taking the input (argument or parameter) after it
    // \ - the escape sequences
    // \n - creates a new line/line break
    // ; - finishes the line of code
}

// side notes

// strings - basically text, sequences of characters

// \ - more escape sequences
    // \r - goes to the beginning of the line
    // \" - makes double quote
    // \' - makes single quotes
    // \\ - makes backslashes

// bool - true or false
// char - single characters
// double and float - real numbers (with decimal points)
// long - long integers

// format codes - basically placeholders
    // %s - asking to 'put another string in the following'
    // %i - for integers

// conditionals - decide which way to go based on a question
    // if
    // else
    // else if - else and then if conditional

// operators
    // = - assign a return value from the right side to the left side
    // < - less than
    // <= - less than or equal
    // > - greater than
    // >= - greater than or equal
    // == - equal
    // != - different

// variables